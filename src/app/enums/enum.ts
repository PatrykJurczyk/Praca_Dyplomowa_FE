export enum UserStorage {
  TOKEN_KEY = 'auth-token',
  USER_KEY = 'auth-user',
  USER_ROLE = 'auth-user-role',
}

export enum isReserved {
  niezarezerwowany = 0,
  doRezerwacji = 1,
  zarezerwowany = 2,
  archiwizowany = 3,
}
